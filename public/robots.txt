# robots.txt — thegrowthpmm.com
#
# Policy: allow everything. This is a marketing site whose goal is maximum
# discovery, so every crawler — search engines AND AI bots (answer engines
# and training crawlers alike) — is welcome to read and cite the content.
#
# This deliberately permits AI training too (GPTBot, Google-Extended, etc.).
# For a brochure site, being read/cited by ChatGPT, Gemini, Perplexity and
# Claude is worth far more than opting out of training datasets.
#
# NOTE: This file only takes effect while Cloudflare's "Manage your robots.txt"
# setting is turned OFF ("Disable robots.txt configuration"). If it gets set
# back to "Instruct AI bots to not scrape content," Cloudflare serves its own
# managed robots.txt at the edge and this file is ignored.

User-agent: *
Allow: /
